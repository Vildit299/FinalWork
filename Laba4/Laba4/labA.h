#ifndef LABA_H
#define LABA_H

#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <locale.h>

void inputA(int* A);
void printA(const int* A);
int  processA(const int* A, int k1);
void printResultA(int k1, int sum);

#endif
